function solve(comp, twist, pages) {

    comp = Number(comp);
    twist = Number(twist);
    pages = Number(pages);
    let result = '';
    let isFound = false;

    // if (){

    // }

}

solve(80,
    40,
    3
)